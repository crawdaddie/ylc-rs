let printf = fn (fmt: str, ...): int 

# let a = [501.000001, 502.01002 + 2, 503.20, 2.0]
let a = [501]

let compare = fn (a, bf) {
  bf(a)
}

let bff = fn (a) {
  printf("hello\n")
}

printf("%d\n", compare(a, bff)) 
