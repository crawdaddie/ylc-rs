let printf = fn (fmt: str, ...): int 

let a = [501.000001, 502.01002 + 2, 503.20]

let print_el = fn (el) {
  printf("%f\n", el)
}

let print_arr = fn (a) {
  printf("[%f %f %f]\n", a[0], a[1], a[2])
}

print_arr(a)


