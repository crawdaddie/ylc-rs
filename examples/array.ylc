let printf = fn (fmt: str, ...): int 


let a = [501.000001, 502.01002 + 2, 503.20, 2.0]

let print_el = fn (el) {
  printf("%f\n", el)
}

#let print_arr = fn (a) {
#  printf("[%p %f %f %f %d]\n", a, a[0], a[1], a[3])
#}

let print_arr = fn (a) {
  
  
  printf("[%p %f %d]\n", a, a[3])
}

print_arr(a)


